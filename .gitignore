volatile/
_THIS_IS_*_ENV_
db/*
logs/*
tmp/*
venv*
*.tmp
tmp.*
*.swp
.python-version
*.sqlite

# VSC, Python, and Mac-centric items follow
*.pyc
*.pyo
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
**/.ipynb_checkpoints/

# macOS system files
.DS_Store
.AppleDouble
.LSOverride
._*
**/.DS-Store

.venv
aws_rootkey_confidential.csv
**/tmp*.pdf
.chainlit/*

# Python package build artifacts
*.egg-info/
*.egg-info/*
# Root-anchored: these are what `python -m build` / `uv build` create at the repo
# root. Anchoring keeps them from masking a source directory named build/ nested
# somewhere in the tree.
/build/
/dist/

# Accidental nested git metadata (must never be part of the package tree)
src/totodev_pub/.git/

pypi_token

# Git worktrees used for isolated agent-driven implementation work
.worktrees/

# Working design docs from agent-driven planning (kept local, not shipped)
docs/superpowers/
